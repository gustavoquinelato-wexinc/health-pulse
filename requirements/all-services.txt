# Common specific dependencies
-r common.txt

# Auth Service specific dependencies
-r auth-service.txt

# Backend Service specific dependencies (includes ETL)
-r backend-service.txt

# Note: etl-service.txt is kept for reference only
# ETL functionality is now integrated into backend-service