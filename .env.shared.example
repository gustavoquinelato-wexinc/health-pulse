# =============================================================================
# SHARED PLATFORM ENVIRONMENT TEMPLATE
# =============================================================================
# 🔒 SECURITY: This is a TEMPLATE file - safe to commit
# Copy to .env.shared and customize with your infrastructure settings
#
# SETUP: cp .env.shared.example .env.shared

# =============================================================================
# DATABASE CONFIGURATION (Shared by all services)
# =============================================================================
# PostgreSQL Database
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=pulse_user
POSTGRES_PASSWORD=your-database-password-here
POSTGRES_DATABASE=pulse_db

# Database Pool Settings
POSTGRES_POOL_SIZE=20
POSTGRES_MAX_OVERFLOW=30

# =============================================================================
# REDIS CONFIGURATION (Shared sessions and caching)
# =============================================================================
# Redis for session management and caching
REDIS_URL=redis://localhost:6379/0
CACHE_TTL_SECONDS=3600

# =============================================================================
# SERVICE COMMUNICATION URLS
# =============================================================================
# Backend Service (Authentication & API)
BACKEND_SERVICE_URL=http://localhost:3001

# ETL Service (Data Processing)
ETL_SERVICE_URL=http://localhost:8000

# Frontend App (User Interface)
FRONTEND_SERVICE_URL=http://localhost:3000

# AI Service (Future)
AI_SERVICE_URL=http://localhost:8001

# =============================================================================
# CORS CONFIGURATION
# =============================================================================
# Allowed origins for CORS (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# =============================================================================
# LOGGING CONFIGURATION
# =============================================================================
# Global logging settings
LOG_LEVEL=INFO
LOG_FORMAT=json

# =============================================================================
# NOTES
# =============================================================================
# 1. This file contains infrastructure configuration shared by all services
# 2. Database and Redis settings are used by Backend and ETL services
# 3. Service URLs are used for inter-service communication
# 4. Safe to commit (no secrets, only infrastructure settings)
# 5. Service-specific secrets are in separate .env files
# 6. Update POSTGRES_PASSWORD with your actual database password
