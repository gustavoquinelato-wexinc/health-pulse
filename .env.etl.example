# =============================================================================
# ETL SERVICE CLIENT-SPECIFIC ENVIRONMENT TEMPLATE
# =============================================================================
# 🔒 SECURITY: This is a TEMPLATE file - safe to commit
# Copy to .env.etl.{client} and customize with client-specific configuration
#
# SETUP EXAMPLES:
#   cp .env.etl.example .env.etl.wex
#   cp .env.etl.example .env.etl.techcorp
#   cp .env.etl.example .env.etl.acme

# =============================================================================
# CLIENT CONFIGURATION
# =============================================================================
# CLIENT_NAME is looked up in database (case-insensitive)
# This must match a client name in the database
CLIENT_NAME=YOUR_CLIENT_NAME

# =============================================================================
# ETL SERVICE CONFIGURATION
# =============================================================================
# Service Settings
ETL_PORT=8000
ETL_HOST=0.0.0.0
DEBUG=false
LOG_LEVEL=INFO

# Job Scheduling
ORCHESTRATOR_INTERVAL_MINUTES=60
SCHEDULER_TIMEZONE=UTC

# =============================================================================
# ETL-SPECIFIC SECURITY SECRETS
# =============================================================================
# Encryption key for API tokens stored in database
ENCRYPTION_KEY=your-32-byte-encryption-key-here

# General Security
SECRET_KEY=your-secret-key-change-this-in-production

# =============================================================================
# INTEGRATION API TOKENS (Client-Specific)
# =============================================================================
# Jira Integration
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=your-email@company.com
JIRA_API_TOKEN=your-jira-api-token

# GitHub Integration
GITHUB_TOKEN=your-github-personal-access-token

# Azure DevOps Integration (Optional)
AZDO_URL=https://dev.azure.com/your-org
AZDO_TOKEN=your-azure-devops-token

# Aha! Integration (Optional)
AHA_URL=https://your-domain.aha.io
AHA_TOKEN=your-aha-api-token

# =============================================================================
# MULTI-INSTANCE CONFIGURATION
# =============================================================================
# For running multiple ETL instances for different clients:
# 1. Copy this file to .env.etl.{client_name}
# 2. Update CLIENT_NAME to match database client
# 3. Update ETL_PORT to avoid conflicts (8000, 8001, 8002, etc.)
# 4. Add client-specific API tokens above
# 5. Start with: CLIENT_NAME={client} python -m uvicorn app.main:app --port {port}

# =============================================================================
# NOTES
# =============================================================================
# 1. This file contains client-specific API tokens and configuration
# 2. Each client should have their own .env.etl.{client} file
# 3. CLIENT_NAME must match a client in the database
# 4. API tokens are encrypted before storage in database
# 5. Never commit this file to version control
# 6. Database and Backend Service configuration is in .env.shared
# 7. ETL Service authenticates through Backend Service (no JWT secrets needed)
