# =============================================================================
# FRONTEND APP - ENVIRONMENT CONFIGURATION
# =============================================================================
# 🔒 SECURITY: Contains NO secrets - only public configuration
# Used by: Frontend React App (Vite build process)

# =============================================================================
# SERVICE IDENTIFICATION
# =============================================================================
APP_NAME=Pulse Frontend App
SERVICE_NAME=frontend

# =============================================================================
# VITE CONFIGURATION (PUBLIC - EXPOSED TO BROWSER)
# =============================================================================
# API Endpoints
VITE_API_BASE_URL=http://localhost:3002
VITE_ETL_SERVICE_URL=http://localhost:8000
VITE_AI_SERVICE_URL=http://localhost:8001

# Application Settings
VITE_APP_TITLE=Pulse Platform
VITE_APP_VERSION=1.0.0
VITE_ENVIRONMENT=development

# Feature Flags
VITE_ENABLE_REAL_TIME=true
VITE_ENABLE_AI_FEATURES=true
VITE_ENABLE_DARK_MODE=true
VITE_ENABLE_ANALYTICS=true

# Development Settings
VITE_DEBUG_MODE=true
VITE_SHOW_DEV_TOOLS=true

# =============================================================================
# BUILD CONFIGURATION
# =============================================================================
# Vite build settings
VITE_BUILD_TARGET=es2020
VITE_BUILD_OUTDIR=dist

# =============================================================================
# NOTES
# =============================================================================
# 1. All VITE_ variables are PUBLIC and exposed to the browser
# 2. NEVER put secrets in this file
# 3. This file is safe to commit to version control
# 4. Frontend gets service URLs but no authentication secrets
# 5. Authentication is handled by Backend Service via cookies/tokens
