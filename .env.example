# =============================================================================
# PULSE PLATFORM - ENVIRONMENT TEMPLATE
# =============================================================================
# 🔒 SECURITY: This is a TEMPLATE file - safe to commit
# Copy and customize the appropriate files for your environment

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Customize .env.shared for your environment (database, URLs)
# 2. Copy .env.backend.example to .env.backend and add secrets
# 3. Copy .env.etl.wex.example to .env.etl.wex and add WEX API tokens
# 4. Copy .env.etl.techcorp.example to .env.etl.techcorp and add TechCorp API tokens
# 5. Copy .env.frontend (usually no changes needed)

# =============================================================================
# QUICK SETUP COMMANDS
# =============================================================================
# Development setup:
#   cp .env.backend.example .env.backend
#   cp .env.etl.wex.example .env.etl.wex
#   
#   # Then edit the files to add your actual secrets

# Multi-instance setup:
#   cp .env.etl.wex.example .env.etl.wex
#   cp .env.etl.techcorp.example .env.etl.techcorp
#   
#   # Then edit the files to add client-specific API tokens

# =============================================================================
# SECURITY NOTES
# =============================================================================
# ✅ .env.shared - Safe to commit (no secrets)
# ✅ .env.frontend - Safe to commit (public config only)
# ❌ .env.backend - NEVER commit (contains JWT secrets)
# ❌ .env.etl.* - NEVER commit (contains API tokens)
# ❌ .env.production* - NEVER commit (production secrets)

# =============================================================================
# FILE DESCRIPTIONS
# =============================================================================
# .env.shared      - Database URLs, service URLs (shared across all services)
# .env.backend     - JWT secrets, session keys (Backend Service only)
# .env.etl.wex     - WEX-specific ETL configuration and API tokens
# .env.etl.techcorp - TechCorp-specific ETL configuration and API tokens
# .env.frontend    - Public configuration (Frontend App only)

# =============================================================================
# DOCKER COMPOSE USAGE
# =============================================================================
# Development (defaults to WEX):
#   docker-compose -f docker-compose.dev.yml up -d
#
# Multi-instance production:
#   docker-compose -f docker-compose.multi-client.yml up -d
